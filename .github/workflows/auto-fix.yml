name: Auto Fix

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  check_run:
    types: [completed]

jobs:
  auto-fix:
    uses: wopr-network/.github/.github/workflows/auto-fix.yml@main
    secrets: inherit
